export default async function handler(req, res) {
    if (req.query.gs) {
        // console.log(req.query.gs)
        // parse gs url into two piece and return
    }

    res.status(200).json({a:'b'})
}